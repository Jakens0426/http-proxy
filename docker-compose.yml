services:
  http-proxy:
    build: .
    container_name: http-proxy
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
      - LISTEN_ADDR=0.0.0.0:9090
